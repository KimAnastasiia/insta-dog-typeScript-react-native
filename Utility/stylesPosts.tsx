/* eslint-disable prettier/prettier */

import {StyleSheet} from 'react-native';

const stylesPosts = StyleSheet.create({
    avatar: {
      borderRadius: 50,
      width:90,
      height:90,
    },
    heading: {
      fontSize:25,
      fontWeight:'bold',
      textAlign:"center"
    },
    title: {
      textAlign:'center',
      fontWeight:'bold',
    },
});

export default stylesPosts;